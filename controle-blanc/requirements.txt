fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
# Importé directement par app/schemas.py. FastAPI l'installe déjà, mais une
# dépendance qu'on importe se déclare : on ne compte pas sur le hasard d'une
# dépendance transitive.
pydantic>=2.0
anthropic>=1.0
# Web Push : les rappels de contrôle (app/rappels.py). Aucun coût par message
# — c'est le mécanisme du navigateur, pas un service tiers.
pywebpush>=2.0
# Installées par pywebpush, mais importées directement par nos outils
# (cles_vapid, verifier_rappels) : une dépendance qu'on importe se déclare.
cryptography>=42.0
py-vapid>=1.9
